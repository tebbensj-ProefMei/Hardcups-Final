# Database setup
# Choose which database backend to use.
# - sqlite  (default): stores data in backend/proefmei.db, no server required.
# - mysql   : connect to an external MySQL server using the credentials below.
DB_BACKEND=sqlite

# MySQL 8 credentials (ignored when DB_BACKEND=sqlite)
DB_USER=root
DB_PASS=yourpassword
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=proefmei

# Optional custom SQLite path. Leave commented to use backend/proefmei.db.
# You can point this to a different folder to share a database with multiple
# developers or to keep the file outside de repository.
# SQLITE_DB_PATH=/absolute/or/relative/path/to/proefmei.db

# You can also provide a full DATABASE_URL instead of the options above.
# Example for MySQL 8: DATABASE_URL=mysql+pymysql://user:pass@host:3306/proefmei
# Example for SQLite absolute path: DATABASE_URL=sqlite:////tmp/proefmei.db

# JWT secret
JWT_SECRET=choose_a_long_random_secret
