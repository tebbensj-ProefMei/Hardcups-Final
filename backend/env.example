# Database setup
# Kies welke database je gebruikt.
# - mysql   : voor productie, koppeling met een MySQL 8-server (aanbevolen).
# - sqlite  : alleen voor lokale ontwikkeling/testen.
DB_BACKEND=mysql

# MySQL 8 credentials (genegeerd wanneer DB_BACKEND=sqlite)
DB_USER=hardcups_app
DB_PASS=sterkWachtwoord!
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=hardcups

# Optioneel: alternatief SQLite-pad. Laat commentaar staan voor standaard.
# SQLITE_DB_PATH=backend/proefmei.db

# Je kunt ook een volledige DATABASE_URL opgeven in plaats van bovenstaande
# opties. Voorbeeld: mysql+pymysql://user:pass@host:3306/hardcups

# JWT secret
JWT_SECRET=choose_a_long_random_secret
